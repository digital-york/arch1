<!DOCTYPE html>

<html lang="en" class="no-js">

<head>
  <meta charset="utf-8"/>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  <title>Archbishops&#39; Registers</title>
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
</head>

<body>

<div id="container">

  <div id="header">

    <div id="header_text">
      <%= link_to "Archbishops&#39; Registers Editing Tool".html_safe, entry_path("default") %>
    </div>

  <span style="float: right; margin-right: 6px; margin-top: 25px">
    <%= button_to 'Logout', logout_path, :method => 'get', :data => {:confirm => 'Are you sure you want to logout?'} %>
  </span>

  </div>

  <div id="navbar">

    <span style="float: left"><a href='/landing_page'><%= image_tag 'back_icon2.png', class: 'home_icon', :title => 'Back to Registers' %></a></span>

    <span style="float: left">&nbsp;&nbsp;<strong>Current Register</strong>: <span style="font-size: 0.95em; color: #FFFFFF"><%= session[:register_name] %></span></span>

    <form style="float: left" name="myForm" id="choose_folio">
      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<strong>Current Folio</strong>:
      <select name="folio_id" style="width: 260px" class="choose_folio"
              <% if controller.action_name.to_s != 'index' %>disabled
              <% end %>>
        <option value="">--- select folio ---</option>
        <% @folio_list.each do |folio| %>
            <option value="<%= folio[0] %>"
                    <% if session[:folio_id] == folio[0] %>selected
                    <% end %>><%= folio[1] %></option>
        <% end %>
      </select>
      <input type="hidden" name="set_folio" value="true"/>
    </form>
    </span>

  </div>

  <div id="main">
    <%= yield %>
  </div>

  <div style="clear: both"></div>

</div>

</body>
</html>

