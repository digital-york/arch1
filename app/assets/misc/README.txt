- The two files in this directory create and drop the database tables.
- They can be run as migrations.
- They might be a bit out of date so it would be a good idea to check them before running the migrations.
- In general, it is better to make sure the correct schema.db file exists in the db directory and to run the 'rake db:schema:load' command.
