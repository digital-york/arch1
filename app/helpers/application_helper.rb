module ApplicationHelper

  # Simple method to capitalise the first letter of a string
  # Could probably just put this directly in the code but I think
  # it makes it easier to understand putting the code in this method
  def capitalize_first_letter(string)
    return string.titleize
  end

end
